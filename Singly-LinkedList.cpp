// LinkedList with 3 elements
#include <iostream>
using namespace std;

class Node
{
public:
    int Value;
    Node *Next;
};

int main()
{
    Node *head = new Node(); // create pointer and alocate space for first node
    Node *second = new Node();
    Node *third = new Node();

    return 0;
}